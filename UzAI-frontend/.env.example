# Backend API (used by Vite dev server proxy). Restart dev server after changing.
# - dotnet run (default): leave unset or 5206
# - Docker (docker-compose): set to 8080
# API_PROXY_TARGET=http://localhost:8080
